import { Component, Output, Input, EventEmitter, HostListener, ViewChild, ElementRef, Renderer } from '@angular/core';

@Component({
  selector:'my-footer',
  templateUrl: './footer.html',
  styleUrls: ['./footer.less', '../../site.styles.less']
})
export class FooterComponent {
      
}